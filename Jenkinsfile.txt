pipeline {
    agent any
    stages {
        stage('Build Docker Image') {
            steps {
                sh 'docker build -t plant-disease-app .'
            }
        }
        stage('Run Container') {
            steps {
                sh 'docker run -d -p 8501:8501 plant-disease-app'
            }
        }
    }
}
